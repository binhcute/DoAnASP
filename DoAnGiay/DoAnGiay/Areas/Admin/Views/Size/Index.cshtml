@model IEnumerable<DoAnGiay.Areas.Admin.Models.SizeModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumSize)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumSize)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSize">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSize">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSize">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
